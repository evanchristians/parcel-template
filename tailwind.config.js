module.exports = {
    purge: ["./src/**/*.html"],
    darkMode: false, // or 'media' or 'class'
    theme: {
        minHeight: {
            "error-field": "15px",
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
